export type BookTypes = {
    title: string,
    type: string,
    code: string,
    price: string,
    download: string,
    isBought?: boolean,
    isFavorite?: boolean,
    isRecommend?: boolean,
    isLoved?: boolean,
    isLovedState?: boolean
  }
  